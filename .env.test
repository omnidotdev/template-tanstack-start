# Test environment configuration
# API endpoints are mocked via MSW

VITE_BASE_URL="https://localhost:3000"
VITE_API_BASE_URL="https://localhost:4000"
VITE_AUTH_BASE_URL="https://localhost:8000"

# OAuth client credentials (mocked)
AUTH_CLIENT_ID="test-client-id"
AUTH_CLIENT_SECRET="test-client-secret"
AUTH_SECRET="test-secret-for-testing-only"

# Stripe (mocked)
STRIPE_API_KEY="sk_test_mock"

# Disable TLS validation for tests
NODE_TLS_REJECT_UNAUTHORIZED="0"
