[test]
env = ".env.test"
timeout = 30000
preload = ["./src/test/preload.ts"]
coverageSkipTestFiles = true

# exclude E2E tests from `bun test` (they use Playwright)
testIgnorePatterns = ["**/e2e/**"]
